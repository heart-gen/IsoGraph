# `isograph.models.vae`

The variational-autoencoder network-inference backend used for the primary IsoGraph
analyses. Documents the encoder/decoder configuration, fitting loop, and the
`FitArtifacts` produced for downstream module extraction.

```{eval-rst}
.. automodule:: isograph.models.vae
   :members:
   :undoc-members:
```
