"""Annotation and consequence integration (Stage 8C — stub)."""

from __future__ import annotations


def annotate_driver_table(gene_driver_table, annotation_table):
    """Merge consequence/annotation columns into gene_driver_table. Returns DataFrame."""
    raise NotImplementedError("Stage 8C")


def annotate_transcript_table(transcript_polarity_table, annotation_table):
    """Merge splice event annotations into transcript_polarity_table. Returns DataFrame."""
    raise NotImplementedError("Stage 8C")
